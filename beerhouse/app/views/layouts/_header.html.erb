<header>
  <h1>Beer House</h1>
  <div>
    <nav>
      <ul>
        <% if logged_in? %>
          <li> <%= link_to "Log out", sign_out_path, method: "delete" %> </li>
        <% else %>
          <li><%= link_to "Log in", root_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
